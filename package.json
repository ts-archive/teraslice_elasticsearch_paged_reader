{
  "name": "teraslice_elasticsearch_paged_reader",
  "version": "0.0.1",
  "description": "A simple Teraslice paged reader for elasticsearch. Allows sorted queries with a single worker on modestly sized data sets.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.0"
  }
}
